export const patterns = {
  naturalNumbers: /^\d+$/,
  wholeNumbers: /^-?\d+$/,
  positiveDecimalNumbers: /^\d*(\.\d+)?$/,
  decimalNumbers: /^-?\d*(\.\d+)?$/,
  alphabets:/^[a-zA-Z]*$/,
  alphanumeric: /^[a-zA-Z0-9]*$/
}
